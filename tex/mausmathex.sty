% .sty for mathematics exercise answers and reports
% use XeLaTeX or LuaLaTex for utf-8 support (fontspac)
% not a very polished package

\ProvidesPackage{mausmathex}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{mathtools}
\usepackage{amsthm}

\usepackage{unicode-math}
\usepackage{fontspec}

%font with ligatures
% latin modern:

\setmainfont[Ligatures=TeX]{Latin Modern Roman}

% XITS (Times like)
%\setmainfont[Ligatures=TeX]{XITS}

% Tex Gyre Pagella (Palatino like)
%\setmainfont[Ligatures=TeX]{Tex Gyre Pagella}

% Corresponding math fonts

% latin modern
\setmathfont[math-style=TeX]{Latin Modern Math}
\AtBeginDocument{\let\setminus\smallsetminus} % workaround a \setminus = (no glyph) unicode-math / lmodern bug

% XITS
%\setmathfont[math-style=TeX]{XITS Math}

% Asana (Palatino like)
%\setmathfont[math-style=TeX]{Asana Math}

\usepackage{setspace}   % control spacing if needed
%\onehalfspacing

\usepackage{microtype} % does microtype have Xe / LuaLaTex support? yes

\usepackage{polyglossia}
\setdefaultlanguage{english}
%\setdefaultlanguage{finnish}

\usepackage{listings}          % Code listings

\usepackage{color}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{hyperref}

\usepackage{placeins}   % provides \FloatBarrier

\lstset{                       % Adjust code listings
    extendedchars=true,
    inputencoding=utf8,
    showstringspaces=false,
    basicstyle=\footnotesize\ttfamily
}

\usepackage{parskip}

%% custom theorem env's (in Finnish)

\theoremstyle{definition}
\newtheorem{maar}{Määritelmä}

\theoremstyle{definition}
\newtheorem{esim}{Esimerkki}

\theoremstyle{plain}
\newtheorem{lause}{Lause}

\theoremstyle{plain}
\newtheorem{lemma}{Lemma}

\theoremstyle{remark}
\newtheorem{huom}{Huomautus}

\theoremstyle{remark}
\newtheorem*{merk}{Merkintä}

% additional
\theoremstyle{plain}
\newtheorem*{todo}{Todo}

\theoremstyle{plain}
\newtheorem{kysymys}{Kysymys}

\theoremstyle{remark}
\newtheorem*{pohdi}{Pohdi}


%% symbols and notation:

% common
\newcommand{\R}{\mathbb{R}}
\newcommand{\Natural}{\mathbb{N}}
\newcommand{\Integer}{\mathbb{Z}}
\newcommand{\Complex}{\mathbb{C}}
\newcommand{\euler}{e}
\newcommand{\iu}{i}

\DeclareMathOperator*{\argmin}{arg\,min}
\DeclareMathOperator*{\argmax}{arg\,max}


\DeclareMathOperator*{\sgn}{sgn}

% dx for integrals
\newcommand{\df}[1]{\, \mathrm{d} #1}

% set mark-up and related
\DeclarePairedDelimiterX\Set[2]{\lbrace}{\rbrace}{ #1 \,\delimsize|\, #2 }
\DeclarePairedDelimiterX\PlainSet[1]{\lbrace}{\rbrace}{ #1 }

% gcd
\DeclareMathOperator{\syt}{syt} % fi

% vector notation
\newcommand{\vect}[1]{\ensuremath{\mathbf{#1}}}
\newcommand{\inprod}[1]{\ensuremath{\langle #1 \rangle}}

% CS notation thingys
\newcommand{\BigO}[1]{\ensuremath{\mathcal{O}\left(#1\right)}}

% statistics
\newcommand*\mean[1]{\overline{#1}}
\newcommand{\Var}{\mathrm{Var}}
\newcommand{\Cov}{\mathrm{Cov}}
\newcommand{\E}{\mathrm{E}}
\newcommand{\corr}{\mathrm{corr}}

% matrix algebra
\newcommand{\rank}{\mathrm{rank}}
\newcommand{\mydet}[1]{\mathrm{det}\left( #1 \right)}

% norms, abs
%\providecommand{\abs}[1]{\lvert #1 \rvert}
\DeclarePairedDelimiter\abs{\lvert}{\rvert}
\providecommand{\norm}[1]{\lVert #1 \rVert}

% machine learning
\newcommand{\Entropy}{\mathrm{Entropy}}
\newcommand{\Gini}{\mathrm{Gini}}
\newcommand{\ClassifErr}{\mathrm{Classification Error}}

%% Custom reference with nameref:

\newcommand{\customref}[1]{\ref{#1}.~''\nameref{#1}''}
